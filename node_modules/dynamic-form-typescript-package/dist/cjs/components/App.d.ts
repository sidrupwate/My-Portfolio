import React, { ChangeEvent, Component, FormEvent } from "react";
interface FormField {
    label: string;
    name: string;
    type: string;
    value: string;
    fieldoption?: {
        name: string;
        value: number;
    }[];
    gridSizes?: {
        xs?: number;
    };
}
interface MyFormProps {
    form: object[];
    formArray: FormField[];
    handleChange: (e: ChangeEvent<HTMLInputElement | {
        value: unknown;
    }>, key: string) => void;
    handleSubmit: (e: FormEvent<HTMLButtonElement>) => void;
}
declare class MyForm extends Component<MyFormProps> {
    renderFormFields(): (React.JSX.Element | null)[];
    render(): React.JSX.Element;
}
export default MyForm;
